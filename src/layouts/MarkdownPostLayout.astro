---
import BaseLayout from './BaseLayout.astro';
import '../styles/markdown.css';
const { frontmatter } = Astro.props;

const formattedDate = new Date(frontmatter.pubDate).toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<BaseLayout pageTitle={frontmatter.title}>
  <div class="flex flex-col items-center justify-center mb-10">
    <h1 class="my-4">{frontmatter.title}</h1>
    <p class="my-1 text-gray-500 dark:text-gray-400">{frontmatter.description}</p>
    <p class="my-1 text-gray-500 dark:text-gray-400 text-sm">{formattedDate}</p>
    <div class="img-container">
      <img src={frontmatter.image.url} alt={frontmatter.image.alt} class="post-img" />
    </div>
    <p class="text-gray-500 dark:text-gray-400">{frontmatter.image.alt}</p>
  </div>
  <div class="markdown-content">
    <slot />
  </div>
</BaseLayout>
